# Voice-census fixture (stable — keep read-only; the integration test pins its counts)

You must never edit the core directly.
Do not bypass the gate.
The Scout may not add files.
This sentence is purely affirmative and reads cleanly.
Keep at most 3 files — never exceed that hard limit.
